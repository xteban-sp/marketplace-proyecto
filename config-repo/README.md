# Config Repo Local

Configuraciones centralizadas consumidas por `config-service` usando backend `native`.

- Nombre de archivo por servicio: `<spring.application.name>.properties` o `.yml`
- Ubicacion usada por Config Server: `file:./config-repo`
